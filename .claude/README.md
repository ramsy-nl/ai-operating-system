# .claude/

Claude Code project configuration lives here (e.g. `settings.json`, custom
slash commands under `commands/`, hooks).

The **operating model** is not configured here — it lives in `AGENTS.md` at the
repo root, surfaced to Claude via `CLAUDE.md`. Keep behavioural rules there so
every tool stays in sync; use this directory only for Claude-specific tooling
config.
